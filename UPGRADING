-------------------------------------------------------------------------------
4.x -> 5.x (PHP 8.3 modernization)

1. Backward Incompatible Changes
2. New Features

========================================
1. Backward Incompatible Changes
========================================

- PHP: minimum supported version is now `>= 8.3` (was `>= 8.1`).
- `BackQ\Adapter\AbstractAdapter::hasWorkers($queue)` is narrowed to `bool`
  (was `bool|int|null`). Concrete adapters return real values where the backend
  supports the check and `false` elsewhere: `Redis`, `Nsq` and `DynamoSQS`
  report "no workers" (a documented stub contract), `Beanstalk` implements the
  check against real tube stats. Subclasses overriding `hasWorkers()` must
  declare a `bool` return type.
- `BackQ\Adapter\AbstractAdapter` method parameters are now typed (were untyped):
  - `bindRead(string $queue)`, `bindWrite(string $queue)`,
    `hasWorkers(string $queue)`
  - `pickTask(?int $timeout = null)`
  - `putTask(string $body, array $params = [])`
  - `afterWorkSuccess(int|string|null $workId)`,
    `afterWorkFailed(int|string|null $workId)`
  - `ping(bool $reconnect = true)`
  Subclasses overriding these methods and callers must match the new argument
  types.
- `BackQ\Publisher\AbstractPublisher::ready()` is narrowed to `bool` (was
  untyped; it returned `null` before `start()`). The bound path is unchanged;
  callers and subclasses overriding `ready()` must use a `bool` return type.
- `BackQ\Worker\AbstractWorker::$workTimeout` is now typed `?int` (was untyped,
  default `null`). Child classes that assign an `int` (such as the SNS workers'
  `= 5`) must declare `?int $workTimeout`.
- Deprecated/dead API removed:
  - `BackQ\Worker\AbstractWorker::debug()` (use `logDebug()`)
  - NSQ `info()`/`error()` aliases on `BackQ\Adapter\Nsq` (use the PSR-3
    `logInfo()`/`logError()` methods)
  - `BackQ\Adapter\Beanstalk::pickTasks()` (dead, unused)
- `BackQ\Message\Generic` keeps `Serializable` for backward compatibility but
  now also implements the native `__serialize()`/`__unserialize()` magic
  methods (removing the PHP 8.1+ interface deprecation). The legacy
  `serialize()`/`unserialize()` methods are `@deprecated`. Payloads produced by
  4.x (`C:` capsules) are still read back; new payloads serialize in the native
  `O:` format.
- Dependency floors raised (see `composer.json`): `symfony/console` is now a
  declared runtime dependency (was a transitive, undeclared one).
- `#[Override]` attributes are used on adapter, worker and message method
  overrides (PHP 8.3). Subclasses that override a method that already carries
  the attribute only need to keep their own signatures compatible — the
  attribute is not inherited and is not required on child overrides.

========================================
2. New Features
========================================

- `BackQ\Adapter\ConnectionState`: backed `int` enum (`Nothing` = 0,
  `BindWrite` = 1, `BindRead` = 2) used internally by the `Redis` and `Nsq`
  adapters for the connection state machine. The legacy `STATE_*` integer
  constants on both adapters are kept for backward compatibility.
- `BackQ\Message\Generic::$data` and `getData()` are typed (`mixed`).
- `json_validate()` in place of `@json_decode(...)` + `is_array` checks in
  `BackQ\Adapter\DynamoSQS` and `BackQ\Adapter\Amazon\DynamoDb\QueueTableRow`.

------------------------------------------------------------------------------
3.x -> 4.x (PHP 8.1 modernization)

1. Backward Incompatible Changes
2. New Features

========================================
1. Backward Incompatible Changes
========================================

- PHP: minimum supported version is now `>= 8.1` (was `>= 7.4`).
- Adapter contract (`BackQ\Adapter\AbstractAdapter`):
  - Methods now declare return types. Subclasses/consumers overriding any of
    `connect`, `disconnect`, `bindRead`, `bindWrite`, `pickTask`, `putTask`,
    `ping`, `hasWorkers`, `setWorkTimeout`, `afterWorkSuccess`,
    `afterWorkFailed` must declare compatible (covariant) return types.
  - `pickTask($timeout = null): bool|array` (was untyped)
  - `putTask($body, $params = []): string|int|bool` (was untyped)
  - `ping($reconnect = true): bool` (was untyped)
  - `hasWorkers($queue): bool|int|null` (was untyped; the `$queue` argument is
    now required by the contract, children may keep a default)
  - `connect(): bool`, `disconnect(): bool`, `bindRead($queue): bool`,
    `bindWrite($queue): bool`, `afterWorkSuccess($workId): bool`,
    `afterWorkFailed($workId): bool` (were declared `: void` and contradicted
    every concrete implementation — this previously failed to load on PHP 8)
  - `setWorkTimeout(?int $seconds = null): void` (was untyped)
- `BackQ\Worker\AbstractWorker::$workTimeout` is no longer typed `int` (it is
  plain untyped, default `null`). Child classes must not declare an `int`
  (or `?int`) type for a property they inherit.
- `BackQ\Adapter\Redis\Queue::$retryAfter` / `$blockFor` are untyped (matched
  to the vendored `Illuminate\Queue\RedisQueue` parent).
- `BackQ\Message\Guzzle::$request` is untyped (holds the string form; a psr7 v1
  `Request` object has no `__toString`).
- `BackQ\Message\Amazon\SNS\Application\PlatformEndpoint\PublishMessageInterface::getMessage()`
  is now `: string`.
- `BackQ\Message\Guzzle` now serializes requests via
  `GuzzleHttp\Psr7\Message::toString()` and parses with
  `Message::parseRequest()` (psr7 &ge; 2.7; the `str()`/`parse_request()`
  function shims were removed in psr7 v2).
- Dependency floors raised: `symfony/process` `^5.4 | ^6.4 | ^7.0`,
  `guzzlehttp/psr7` `^2.7`, `psr/log` `^1.1 | ^2.0 | ^3.0`,
  `illuminate/queue`/`illuminate/redis` `^10`. `opis/closure` stays `^3.6`
  (4.x removes the public `SerializableClosure` constructor that
  `BackQ\Message\Closure` relies on).
- Legacy APNs binary protocol support is **removed** (Apple deprecated the
  protocol long ago; APNs stopped supporting the legacy binary interface on
  port 2195). The following classes are deleted, as is the entire
  `duccio/apns-php` dependency:
  - `BackQ\Adapter\ApnsdPush`
  - `BackQ\Worker\Apnsd`
  - `BackQ\Publisher\Apnsd`
  - `BackQ\Message\ApnsPHP`
  - `BackQ\Logger` no longer implements `ApnsPHP_Log_Interface` (its
    `log($sMessage, $debug = false)` API is unchanged).
  - The `example/workers/apnsd.php` and `example/publishers/apnsd.php` examples
    are removed.
  - Replacement: the APNs HTTP/2 provider API, or AWS SNS via
    `BackQ\Publisher\Amazon\SNS\Application\PlatformEndpoint\Publish` /
    `BackQ\Worker\Amazon\SNS\Application\PlatformEndpoint\Publish`.
- Legacy GCM/FCM push support is **removed** (Google shut down GCM in 2019; the
  code built on the ZF1 `Zend_Mobile_Push_*` classes, which are no longer
  shipped). The `Zend_Mobile_Push` dependency is dropped and the following
  classes are deleted:
  - `BackQ\Adapter\Fcm`
  - `BackQ\Message\Fcm`
  - `BackQ\Worker\Fcm`
  - `BackQ\Publisher\Fcm`
  - Replacement: the FCM HTTP v1 API, or AWS SNS FCM platform endpoints via
    `BackQ\Publisher\Amazon\SNS\Application\PlatformEndpoint\Publish` /
    `BackQ\Worker\Amazon\SNS\Application\PlatformEndpoint\Publish`.

========================================
2. New Features
========================================

- Test suite added under `tests/` (PHPUnit 10, `composer app-tests`), with
  coverage of adapters, workers, publishers and messages.
- Constructor property promotion: `BackQ\Adapter\Redis`, `BackQ\Message\Process`,
  `BackQ\Adapter\Amazon\DynamoDb\QueueTableRow` (first param renamed `$payload`),
  `BackQ\Adapter\IO\StreamIO`.
- `str_contains` / `$e::class` / `final` class constants applied throughout
  (`BackQ\Logger`, SNS platform-endpoint workers, `BackQ\Adapter\Nsq`).

-------------------------------------------------------------------------------
2.0.6

1. Backward Incompatible Changes
2. New Features
3. Deprecated Functionality
4. Performance Improvements

========================================
1. Backward Incompatible Changes
========================================

- Publisher:
  - All the `Publisher`'s are now `abstract` classes
  - `__construct` no longer accepts adapter, define `setupAdapter` instead
  - `setupAdapter` is new method that set-up's the adapter instead of constructor
  - `__wakeup` and `__sleep` are now defined on `AbstractPublisher` for serialization

========================================
2. New Features
========================================

Will now keep track of changes in UPGRADING document.

New `Serialized` idea is a worker/queue/publisher that wrap messages from other publishers, currently
this is used to reliably delay long tasts (i.e. something to be scheduled in two weeks), and is used together
with new `DynamoSQS` adapter that saves jobs to AWS DynamoDB and once jobs are ready (TTL triggers) they end up
in SQS queue (DynamoDB->DynamoDB Stream->Lambda->SQS) where they are picked up by worker.
`DynamoSQS` uses DynamoDB for saving and SQS for picking jobs.

- Publisher:
  - New `Serialized` publisher
- Workers:
  - New `Serialized` worker
  - New `Closure`    worker
- Message
  - New `Serialized` message
- Adapter
  - New `DynamoSQS` adapter

========================================
3. Deprecated Functionality
========================================

`Nsq` adapter is no longer maintaned as it's performance is too low. The benefit it has is built-in dashboard
and status of queues. You can continue using it as is.

`toggleDebug()` is removed on workers, they now use standard PSR logger, by default with NORMAL verbose level and
output to console

`debug()` is removed on workers, replaced with `logDebug()`

========================================
4. Performance Improvements
========================================

Refactored internal worker generator loop for handling of empty loops, this affects the APNS worker directly
and possibly others.
Recommended PHP version 7.2 or 7.3.

-------------------------------------------------------------------------------
1.9.13

1. New Features
2. Deprecated Functionality
3. Performance Improvements

========================================
1. New Features
========================================
Summarizing all changes between 1.3 and 1.9.
1.9.13 is last stable and recommended release in version 1.

- Adapter
  - New `NSQ` adapter for nsq.io server (distributed, scalable, has built-in ui)
  - New `Redis` adapter based on illuminate/redis and illuminate/queue

========================================
2. Deprecated Functionality
========================================

`GCM` worker, adapter and message are removed in favor of `Fcm`.

========================================
3. Performance Improvements
========================================

Many bugfixes and improvements, please use latest 1.9 release.
New logger api.



